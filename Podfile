# Uncomment the next line to define a global platform for your project
# platform :ios, '10.0'

target 'BoredAppFirebase' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for BoredAppFirebase

pod 'Firebase/Core'
pod 'FirebaseUI'

  target 'BoredAppFirebaseTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'BoredAppFirebaseUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
